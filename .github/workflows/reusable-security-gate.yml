# Unified security scan — single PR gate covering all CI spec gates.
#
# A single workflow owns the entire PR gate and reports as the required
# status check, instead of several independent modular scan workflows.
#
# Structure:
#   caller-lint       resolves the consumer's caller workflow file from
#                     github.workflow_ref and lints it against the published
#                     contract (contract/security-gate.schema.json) — fail
#                     closed. Every scan job needs it, so a hand-edited caller
#                     that regresses the scaffold invariants never scans.
#   phase1-build      builds the backend image once, generates its image SBOM,
#                     and hands the image to downstream jobs as a transient
#                     `docker save` tarball uploaded via actions/upload-artifact.
#                     No registry round-trip — the image never leaves the run.
#   secrets-scan      TruffleHog (parallel after caller-lint)
#   sast-semgrep      Semgrep (parallel, warn-only until baseline)
#   sast-sonarqube    SonarQube ephemeral (parallel, warn-only until baseline)
#   helm-check        helm lint + template (parallel; blocking unless image_only)
#   cluster-smoke     kind + helm install + GET /health against the built image
#                     (needs: phase1-build) — proves the image actually boots;
#                     skipped when image_only
#   vuln-scan         Trivy (image + source SBOM) + Grype (image + source SBOM
#                     + image SBOM). No Trivy-on-image-SBOM: OS CVEs come from
#                     the direct Trivy image scan; Trivy can't match OS pkgs
#                     from a Syft SBOM. (needs: phase1-build)
#   security-gate     aggregate pass/fail (needs: all, if: always())
#
# Build artifact handling (mirrors the modular workflows' "transient, runner-
# scoped" approach): the image moves between jobs as an uploaded artifact using
# the runner's built-in token. No GHCR push, no `packages: write`, no per-org
# hardcoding, no cleanup job — so this is fork-safe (fork PRs get a read-only
# token and can't push to a registry, but they can upload artifacts).
#
# Blocking:
#   caller-lint, phase1-build, secrets-scan are always blocking.
#   helm-check and cluster-smoke are blocking unless image_only is true
#   (infra/image-only consumers — same skip predicate as the job `if:`).
#   cluster-smoke and vuln-scan gate their hard-fail on the SECURITY_SCAN_BLOCKING
#   repo variable, but the gate still fails if they are skipped/cancelled/errored
#   (e.g. because the build died) — a non-success on any blocking job is a gate
#   failure, so a broken build can never sign off green.
#   sast-semgrep and sast-sonarqube are continue-on-error (warn-only)
#   until the harden baseline lands.

name: Reusable Security Gate

on:
  workflow_call:
    inputs:
      scan_image:
        description: "Local-daemon tag the image is built and scanned under; caller-lint requires it to equal a parsed values-local image string or repository:tag join (comments ignored). Cluster-smoke kind-loads this tag with pullPolicy: Never."
        type: string
        default: app:local
      dockerfile:
        description: "Path to the backend Dockerfile in the consumer repo."
        type: string
        default: containers/backend/Dockerfile
      context:
        description: "Docker build context directory for the primary image; default '.' (repo root). Set to a subdirectory when the Dockerfile expects COPY from that tree (e.g. apps/foo/backend)."
        type: string
        default: .
      target:
        description: "Docker build stage name for the primary image (build-push-action target). Empty (default) builds the last stage — set explicitly when a trailing dev/debug/test stage would otherwise win."
        type: string
        default: ""
      builder_image:
        description: "Build-stage base image, passed as the Dockerfile's BUILDER_IMAGE build ARG; must match the builder base ARG in the consumer's Dockerfile."
        type: string
        default: cgr.dev/chainguard/python:latest-dev
      runtime_image:
        description: "Runtime-stage base image, passed as the Dockerfile's RUNTIME_IMAGE build ARG; must match the runtime base ARG in the consumer's Dockerfile."
        type: string
        default: cgr.dev/chainguard/python:latest
      runtime_apks:
        description: "Extra runtime packages, passed as the Dockerfile's RUNTIME_APKS build ARG; set only when the consumer Dockerfile's runtime stage installs apks."
        type: string
        default: ""
      extra_build_args:
        description: "Newline-separated KEY=VALUE pairs appended to the Docker build-args — for consumer Dockerfiles whose base-image args aren't BUILDER_IMAGE/RUNTIME_IMAGE (e.g. PYTHON_DEV/PYTHON_RUN/NODE_DEV)."
        type: string
        default: ""
      require_hardened_bases:
        description: "Hardened-base policy — operator posture decision, not read from a consumer file. true (default): fail phase1-build when neither Chainguard nor Iron Bank credentials are configured. false: warn and build with the consumer-specified bases (pilot escape hatch — the consumer explicitly owns that posture). When false, Vulnerability Scan labels the run as a public-base / consumer-specified-base proxy scan — a green result is not proof the approved production image is clean."
        type: boolean
        default: true
      ironbank_registry:
        description: "Iron Bank registry host used for docker login whenever IRONBANK_* secrets are set (alongside Chainguard when both are configured). Also used when primary-base failover swaps to ironbank_* images (CGR absent, Iron Bank present). Operator decision; defaults to the DoD registry."
        type: string
        default: registry1.dso.mil
      ironbank_builder_image:
        description: "Optional Iron Bank replacement for builder_image — applied only on primary-base failover (CGR credentials absent AND Iron Bank present). Left empty, the consumer's builder_image passes through. Does not affect whether Iron Bank login runs; login is independent when IRONBANK_* is set."
        type: string
        default: ""
      ironbank_runtime_image:
        description: "Optional Iron Bank replacement for runtime_image — applied only on primary-base failover (CGR credentials absent AND Iron Bank present). Left empty, the consumer's runtime_image passes through. Does not affect whether Iron Bank login runs; login is independent when IRONBANK_* is set."
        type: string
        default: ""
      helm_chart_path:
        description: "Path to the consumer's helm chart directory — the helm lint/template target and the cluster-smoke install source."
        type: string
        default: helm/app
      helm_values_file:
        description: "Path to the consumer chart's base values file, used by helm lint and template."
        type: string
        default: helm/app/values.yaml
      helm_values_local_file:
        description: "Path to the consumer chart's local-overrides values file, layered onto the cluster-smoke helm install. Parsed YAML must pin the backend image to scan_image (string image or repository+tag); comments do not count. Use pullPolicy: Never for cluster-smoke."
        type: string
        default: helm/app/values-local.yaml
      helm_release_name:
        description: "Helm release name for template and the cluster-smoke install; operator choice, no consumer-file counterpart."
        type: string
        default: app-ci
      cluster_name:
        description: "kind cluster name cluster-smoke creates and loads the image into; operator choice, no consumer-file counterpart."
        type: string
        default: app-ci
      namespace:
        description: "Kubernetes namespace cluster-smoke deploys the release into; operator choice, no consumer-file counterpart."
        type: string
        default: app-ci
      secctx_make_target:
        description: "Name of the consumer Makefile target that asserts pod security contexts; skipped with a notice when the consumer Makefile lacks it."
        type: string
        default: security-helm-secctx
      app_path:
        description: "Consumer repo path to the backend app directory, passed as the Dockerfile's APP_PATH build ARG."
        type: string
        default: apps/app/backend
      app_package:
        description: "Backend package dist name, passed as the Dockerfile's APP_PACKAGE build ARG; must match the consumer backend's package metadata."
        type: string
        default: app-backend
      app_module:
        description: "ASGI entrypoint as module:attr, passed as the Dockerfile's APP_MODULE build ARG; must match the consumer backend's app object."
        type: string
        default: app.main:app
      app_port:
        description: "Container port the backend listens on, passed as the Dockerfile's APP_PORT build ARG; must match the consumer chart's backend service/probe port."
        type: string
        default: "8000"
      health_path:
        description: "HTTP path the cluster-smoke step probes on the booted primary image (GET must return 200). Set to the app's health/liveness route, e.g. /health, /healthz, /api/health. Primary deployable only — extra_containers get no cluster smoke."
        type: string
        default: /health
      service_port:
        description: "Port the primary backend Kubernetes Service exposes — used as the cluster-smoke `kubectl port-forward` remote port. This is the Service's .spec.ports[].port, which need not equal app_port; defaults to 8000 (today's behavior)."
        type: string
        default: "8000"
      smoke_workload_match:
        description: "Case-insensitive substring identifying the backend Deployment/Service in cluster-smoke (matched against `kubectl get deploy/svc -o name`). Default 'backend'; set to the workload token for charts whose deployment name contains no 'backend' (e.g. agent-template charts)."
        type: string
        default: backend
      extra_containers:
        description: "JSON array (as a string) of additional containers to build and image-scan beyond the primary deployable — one object per entry: name, dockerfile, context (default '.'), image (default <name>:local), target (optional stage name), build_args (newline-joined KEY=VALUE string). Prefer a multiline YAML '|' block (readable, one object per entry); do not pack the array onto a single quoted line. Default \"\" means no extras. Caller lint validates structure and notices one-line packing; context/image defaults apply in build-extra."
        type: string
        default: ""
      image_only:
        description: "When true, skip helm-check and cluster-smoke (and omit them from the Security Gate blocking set) — for infra/image-only repos that build and vuln-scan images without a deployable app chart. Default false keeps app callers unchanged (helm + smoke still blocking)."
        type: boolean
        default: false
    secrets:
      CGR_PULL_TOKEN:
        required: false
      CGR_PULL_USERNAME:
        required: false
      IRONBANK_TOKEN:
        required: false
      IRONBANK_USERNAME:
        required: false

permissions:
  contents: read
  pull-requests: write

concurrency:
  group: security-scan-${{ github.ref }}
  cancel-in-progress: true

env:
  # Local-daemon tag only — the image is never pushed to a registry. It moves
  # between jobs as a `docker save` tarball (actions artifact), so a bare repo
  # tag is all we need; `docker load` restores this exact tag downstream.
  # MUST match inputs.scan_image to the consumer's helm values-local (backend
  # image + pullPolicy: Never) — cluster-smoke `kind load`s this tag and the
  # chart's backend + migration-hook pods reference it with pullPolicy Never.
  # A mismatch = ErrImageNeverPull and the post-install migration hook hangs
  # until helm times out.
  SCAN_IMAGE: ${{ inputs.scan_image }}
  IMAGE_ARTIFACT: scan-image
  # Base-image args for the backend build. Override builder_image/
  # runtime_image/runtime_apks per consumer if it needs a private Chainguard
  # org or extra runtime packages. The hardened-base login step authenticates
  # Chainguard (CGR_PULL_*) and Iron Bank (IRONBANK_*) independently when each
  # is set (both sessions can be active). Primary builder/runtime swap to
  # ironbank_* images only when CGR is absent and Iron Bank is present; with
  # neither credential, require_hardened_bases decides fail vs warn.
  BACKEND_RUNTIME_APKS: ${{ inputs.runtime_apks }}
  TRIVY_SEVERITY: HIGH,CRITICAL

jobs:
  # --------------------------------------------------------------------------
  # Caller lint — validate the consumer's caller file against the published
  # contract before anything scans. The caller is read from the consumer
  # checkout (github.workflow_ref names it); the lint tooling + contract are
  # checked out at the exact reusable-workflow ref in use
  # (github.job_workflow_sha). Fail closed: an unresolvable caller file or an
  # unreadable caller is a lint failure, never a silent pass.
  # --------------------------------------------------------------------------
  caller-lint:
    name: Caller Lint
    runs-on: ubuntu-latest
    steps:
      # Consumer repo (github.repository = the caller's repo).
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4

      # Resolve which ci-scans ref to lint against. github.job_workflow_sha is
      # empty on pull_request-triggered reusable calls, so prefer the ref the
      # caller actually pins in its `uses:` (parsed from the caller file), then
      # job_workflow_sha, then main. Without this the lint validates against
      # main's contract regardless of the pinned ref — the wrong contract the
      # moment they diverge (e.g. piloting an as-yet-unmerged input).
      - name: Resolve callee (ci-scans) ref
        id: calleeref
        env:
          WORKFLOW_REF: ${{ github.workflow_ref }}
          JOB_WF_SHA: ${{ github.job_workflow_sha }}
        run: |
          caller="${WORKFLOW_REF#"$GITHUB_REPOSITORY/"}"; caller="${caller%%@*}"
          ref=""
          if [ -f "$caller" ]; then
            ref="$(grep -oE 'reusable-security-gate\.yml@[^ ]+' "$caller" | head -1 | sed 's/.*@//')"
          fi
          ref="${ref:-${JOB_WF_SHA:-main}}"
          echo "ref=$ref" >> "$GITHUB_OUTPUT"
          echo "::notice::caller-lint: linting against ci-scans ref '$ref'"

      # Callee tooling (lint script + contract) at the resolved ref.
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
        with:
          repository: c3-joshchiu/c3cdao-ci-scans
          ref: ${{ steps.calleeref.outputs.ref }}
          path: .ci-scans-lint

      - uses: astral-sh/setup-uv@d0cc045d04ccac9d8b7881df0226f9e82c39688e  # v6

      - name: Lint caller against contract
        env:
          WORKFLOW_REF: ${{ github.workflow_ref }}
        run: |
          # WORKFLOW_REF: <owner>/<repo>/<path>@<ref> — strip repo prefix + @ref
          caller="${WORKFLOW_REF#"$GITHUB_REPOSITORY/"}"
          caller="${caller%%@*}"
          if [ ! -f "$caller" ]; then
            echo "::error::caller-lint: cannot resolve caller workflow file from workflow_ref='$WORKFLOW_REF' (resolved '$caller')"
            exit 1
          fi
          uv run .ci-scans-lint/scripts/lib/lint_caller.py "$caller" \
            --contract .ci-scans-lint/contract/security-gate.schema.json \
            --consumer-root "$GITHUB_WORKSPACE"

  # --------------------------------------------------------------------------
  # Phase 1 — build the backend image once and push an ephemeral scan tag
  # --------------------------------------------------------------------------
  phase1-build:
    name: Phase 1 — Docker image build
    runs-on: ubuntu-latest
    needs: [caller-lint]
    outputs:
      image: ${{ env.SCAN_IMAGE }}
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4

      # The backend image is large (WeasyPrint + psql deps). Building it, loading
      # it into Docker, and `docker save`-ing it for the artifact exhausts the
      # hosted runner's ~14 GB free disk. Reclaim ~20 GB of unused toolchains.
      - name: Free disk space
        run: |
          sudo rm -rf /usr/share/dotnet /opt/ghc /usr/local/.ghcup \
            /usr/local/lib/android /opt/hostedtoolcache/CodeQL 2>/dev/null || true
          sudo docker image prune -af 2>/dev/null || true
          df -h /

      - uses: docker/setup-buildx-action@8d2750c68a42422c14e847fe6c8ac0403b4cbd6f  # v3

      # Hardened-base login: authenticate to Chainguard (cgr.dev) when CGR_PULL_*
      # is set AND to Iron Bank (registry1.dso.mil) when IRONBANK_* is set — both
      # sessions can be active at once, so a repo whose images span both registries
      # (e.g. a Chainguard primary with Iron Bank extra_containers) builds in a
      # single run. If only Iron Bank is configured, the primary's builder/runtime
      # optionally swap to the ironbank_* image inputs. With neither,
      # require_hardened_bases decides: fail (default) or warn and build on the
      # consumer-specified bases.
      - name: Login to hardened base registries (Chainguard + Iron Bank)
        id: base-registry
        env:
          CGR_PULL_TOKEN: ${{ secrets.CGR_PULL_TOKEN }}
          CGR_PULL_USERNAME: ${{ secrets.CGR_PULL_USERNAME }}
          IRONBANK_TOKEN: ${{ secrets.IRONBANK_TOKEN }}
          IRONBANK_USERNAME: ${{ secrets.IRONBANK_USERNAME }}
          IRONBANK_REGISTRY: ${{ inputs.ironbank_registry }}
        run: |
          set -uo pipefail
          login_retry() { # <registry> <username> <token>
            local attempt=1 delay=15
            until printf '%s' "$3" | docker login "$1" -u "$2" --password-stdin; do
              if [ "$attempt" -ge 4 ]; then echo "$1 login failed after 4 attempts" >&2; return 1; fi
              echo "$1 login failed (attempt $attempt/4); retrying in ${delay}s..." >&2
              sleep "$delay"; delay=$((delay * 2)); attempt=$((attempt + 1))
            done
          }
          builder='${{ inputs.builder_image }}'
          runtime='${{ inputs.runtime_image }}'
          cgr_ok=0; ib_ok=0
          if [ -n "${CGR_PULL_TOKEN:-}" ]; then
            login_retry cgr.dev "$CGR_PULL_USERNAME" "$CGR_PULL_TOKEN"
            echo "Hardened bases: Chainguard (cgr.dev) authenticated"
            cgr_ok=1
          fi
          if [ -n "${IRONBANK_TOKEN:-}" ]; then
            login_retry "$IRONBANK_REGISTRY" "$IRONBANK_USERNAME" "$IRONBANK_TOKEN"
            echo "Hardened bases: Iron Bank (${IRONBANK_REGISTRY}) authenticated"
            ib_ok=1
          fi
          # Primary base failover: only swap the primary's builder/runtime to the
          # ironbank_* images when Chainguard is absent but Iron Bank is present
          # (preserves the original single-tier behavior). With both authenticated,
          # the primary keeps its Chainguard bases and extra images pull their own
          # Iron Bank bases from the now-logged-in registry.
          if [ "$cgr_ok" = 0 ] && [ "$ib_ok" = 1 ]; then
            [ -n '${{ inputs.ironbank_builder_image }}' ] && builder='${{ inputs.ironbank_builder_image }}'
            [ -n '${{ inputs.ironbank_runtime_image }}' ] && runtime='${{ inputs.ironbank_runtime_image }}'
            echo "::notice::CGR_PULL_TOKEN not configured — using Iron Bank (${IRONBANK_REGISTRY}) for primary bases"
          fi
          if [ "$cgr_ok" = 0 ] && [ "$ib_ok" = 0 ]; then
            if [ '${{ inputs.require_hardened_bases }}' = 'true' ]; then
              echo "::error::No hardened-registry credentials — add CGR_PULL_TOKEN/CGR_PULL_USERNAME (Chainguard) and/or IRONBANK_TOKEN/IRONBANK_USERNAME (Iron Bank), or set require_hardened_bases: false to accept consumer-specified bases."
              exit 1
            fi
            echo "::warning::Proxy-scan posture: no hardened-registry credentials and require_hardened_bases=false — building on consumer-specified bases without a registry login. Vulnerability Scan will cover this gate-reachable image only; a green result is not proof the approved production image is clean."
          fi
          echo "builder_image=$builder" >> "$GITHUB_OUTPUT"
          echo "runtime_image=$runtime" >> "$GITHUB_OUTPUT"

      - name: Label proxy-scan posture
        if: inputs.require_hardened_bases == false || inputs.require_hardened_bases == 'false'
        run: |
          msg='Proxy-scan posture (require_hardened_bases=false): phase1 builds the image with gate-reachable / consumer-specified bases. Downstream Vulnerability Scan is not an approved-prod attestation.'
          echo "::warning::$msg"
          {
            echo "## Proxy-scan posture"
            echo ""
            echo "$msg"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Build scan image (local load, no push)
        uses: docker/build-push-action@10e90e3645eae34f1e60eeb005ba3a3d33f178e8  # v6
        with:
          context: ${{ inputs.context }}
          file: ${{ inputs.dockerfile }}
          target: ${{ inputs.target }}
          push: false
          load: true   # load into local daemon so Syft can scan + we can save it
          tags: ${{ env.SCAN_IMAGE }}
          platforms: linux/amd64
          build-args: |
            BUILDER_IMAGE=${{ steps.base-registry.outputs.builder_image }}
            RUNTIME_IMAGE=${{ steps.base-registry.outputs.runtime_image }}
            APP_PATH=${{ inputs.app_path }}
            APP_PACKAGE=${{ inputs.app_package }}
            APP_MODULE=${{ inputs.app_module }}
            APP_PORT=${{ inputs.app_port }}
            RUNTIME_APKS=${{ env.BACKEND_RUNTIME_APKS }}
            ${{ inputs.extra_build_args }}
          cache-from: type=gha,scope=security-scan-backend
          cache-to: type=gha,mode=max,scope=security-scan-backend
          provenance: false

      # Generate image SBOM while the image is in the local Docker daemon.
      # Syft uses its own registry client, so scanning the local daemon image
      # avoids any registry pull in a downstream job.
      - name: Generate image SBOM (from local daemon)
        uses: anchore/sbom-action@e22c389904149dbc22b58101806040fa8d37a610  # v0.24.0
        with:
          image: ${{ env.SCAN_IMAGE }}
          format: cyclonedx-json
          output-file: sbom-image.json
          artifact-name: sbom-image

      # Hand the built image to downstream jobs as a transient artifact rather
      # than a registry tag. upload-artifact uses the runner's built-in token —
      # no GHCR, no `packages: write`, fork-safe. `docker load` restores the
      # SCAN_IMAGE tag in vuln-scan and cluster-smoke.
      - name: Save image to tarball
        run: docker save "${{ env.SCAN_IMAGE }}" -o image.tar

      - name: Upload image artifact
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02  # v4
        with:
          name: ${{ env.IMAGE_ARTIFACT }}
          path: image.tar
          if-no-files-found: error
          retention-days: 1

  # --------------------------------------------------------------------------
  # Secrets detection — TruffleHog (diff-aware, verified only)
  #
  # Job name MUST stay "TruffleHog verified-secret scan": the main ruleset
  # requires exactly that status-check context (strict policy + merge queue).
  # Renaming it would leave the required context permanently pending on every
  # future PR (including Dependabot). Do not change without updating the ruleset.
  # --------------------------------------------------------------------------
  secrets-scan:
    name: TruffleHog verified-secret scan
    runs-on: ubuntu-latest
    needs: [caller-lint]
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
        with:
          # Full history so the diff base commit is present for range scans.
          fetch-depth: 0

      # Resolve the scan range per event so the scan is diff-aware. Scanning
      # only the PR/merge-queue range (not full history) means a pre-existing
      # verified secret elsewhere in history can't block every future PR once
      # SECURITY_SCAN_BLOCKING=true — the failure mode the modular design avoided.
      - name: Resolve scan range
        id: range
        run: |
          case "${{ github.event_name }}" in
            pull_request)
              echo "base=${{ github.event.pull_request.base.sha }}" >> "$GITHUB_OUTPUT"
              echo "head=${{ github.event.pull_request.head.sha }}" >> "$GITHUB_OUTPUT"
              ;;
            merge_group)
              echo "base=${{ github.event.merge_group.base_sha }}" >> "$GITHUB_OUTPUT"
              echo "head=${{ github.event.merge_group.head_sha }}" >> "$GITHUB_OUTPUT"
              ;;
            *)
              # workflow_dispatch / fallback: full-history scan.
              echo "base=" >> "$GITHUB_OUTPUT"
              echo "head=HEAD" >> "$GITHUB_OUTPUT"
              ;;
          esac

      - name: TruffleHog scan (verified only)
        id: scan
        continue-on-error: true
        uses: trufflesecurity/trufflehog@30d5bb91af1a771378349dbbb0c82129392acf70  # v3.95.6
        with:
          path: ./
          base: ${{ steps.range.outputs.base }}
          head: ${{ steps.range.outputs.head }}
          extra_args: --only-verified

      - name: Gate on verified secrets
        run: |
          if [ "${{ steps.scan.outcome }}" = "failure" ]; then
            if [ "${{ vars.SECURITY_SCAN_BLOCKING }}" = "true" ]; then
              echo "::error::TruffleHog found verified secret(s). Failing (SECURITY_SCAN_BLOCKING=true)."
              exit 1
            fi
            echo "::warning::TruffleHog found verified secret(s). Warn-only; set SECURITY_SCAN_BLOCKING=true to enforce."
          else
            echo "No verified secrets found in the scanned range."
          fi

  # --------------------------------------------------------------------------
  # SAST — Semgrep (warn-only until harden baseline)
  # --------------------------------------------------------------------------
  sast-semgrep:
    name: SAST (Semgrep)
    runs-on: ubuntu-latest
    needs: [caller-lint]
    continue-on-error: true
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
        with:
          fetch-depth: 0

      - name: Install Semgrep
        run: pip install semgrep==1.159.0

      - name: Run Semgrep scan
        id: scan
        run: |
          # Exit codes: 0 = no findings, 1 = findings found (expected),
          # 2+ = real scanner error. Don't swallow real errors with `|| true`.
          set +e
          semgrep scan \
            --config auto \
            --json \
            --output semgrep-results.json \
            --no-git-ignore \
            .
          EXIT_CODE=$?
          set -e
          if [ "$EXIT_CODE" -ge 2 ]; then
            echo "::error::Semgrep failed with exit code $EXIT_CODE"
            exit "$EXIT_CODE"
          fi

      - name: Upload results
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02  # v4
        with:
          name: semgrep-results
          path: semgrep-results.json
          if-no-files-found: warn

      - name: Gate on ERROR-severity findings (warn-only)
        run: |
          set -uo pipefail
          if [ ! -f semgrep-results.json ]; then
            echo "::warning::Semgrep results artifact missing — cannot count ERROR findings"
            exit 1
          fi
          if ! ERRORS=$(jq '[.results[] | select(.extra.severity == "ERROR")] | length' semgrep-results.json); then
            echo "::warning::Semgrep results JSON unreadable — cannot count ERROR findings"
            exit 1
          fi
          if [ "$ERRORS" -gt 0 ]; then
            echo "::warning::Semgrep found ${ERRORS} ERROR-severity finding(s). Warn-only; change exit 0 to exit 1 below to enforce."
            exit 0  # change to `exit 1` to enforce the gate
          fi
          echo "No ERROR-severity Semgrep findings."

  # --------------------------------------------------------------------------
  # SAST — SonarQube ephemeral (warn-only until baseline)
  # --------------------------------------------------------------------------
  sast-sonarqube:
    name: SAST (SonarQube ephemeral)
    runs-on: ubuntu-latest
    needs: [caller-lint]
    timeout-minutes: 30
    continue-on-error: true
    services:
      sonarqube:
        # Hardened base: Iron Bank SonarQube (matches the modular sonarqube.yml
        # CI posture, not a public image). Uses the IRONBANK pull secrets the
        # base repo has. On forks without them the service can't pull and this
        # warn-only (continue-on-error) job errors without blocking the gate.
        image: registry1.dso.mil/ironbank/sonarsource/sonarqube/sonarqube:9.9-community
        credentials:
          username: ${{ secrets.IRONBANK_USERNAME }}
          password: ${{ secrets.IRONBANK_TOKEN }}
        ports:
          - 9000:9000
        env:
          SONAR_ES_BOOTSTRAP_CHECKS_DISABLE: 'true'
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
        with:
          fetch-depth: 0

      - name: Wait for SonarQube
        run: |
          for i in $(seq 1 60); do
            sq_status=$(curl -fsS http://127.0.0.1:9000/api/system/status 2>/dev/null \
              | python3 -c "import sys,json; print(json.load(sys.stdin).get('status',''))" 2>/dev/null || echo "")
            [ "$sq_status" = "UP" ] && echo "SonarQube UP after $((i*5))s" && break
            echo "[$i/60] status: $sq_status"; sleep 5
          done
          [ "$sq_status" = "UP" ] || { echo "::error::SonarQube did not start"; exit 1; }

      - name: Provision project and token
        id: sonar
        run: |
          NEW_PASS="ci-admin-${GITHUB_RUN_ID}"
          curl -fsS -u admin:admin -X POST \
            "http://127.0.0.1:9000/api/users/change_password?login=admin&previousPassword=admin&password=${NEW_PASS}"
          TOKEN=$(curl -fsS -u "admin:${NEW_PASS}" -X POST \
            "http://127.0.0.1:9000/api/user_tokens/generate?name=ci-${GITHUB_RUN_ID}" \
            | python3 -c "import sys,json; print(json.load(sys.stdin)['token'])")
          echo "::add-mask::$TOKEN"
          echo "token=$TOKEN" >> "$GITHUB_OUTPUT"

      - name: Run sonar-scanner
        uses: sonarsource/sonarqube-scan-action@713881670b6b3676cda39549040e2d88c70d582e  # v8.2.0
        env:
          SONAR_TOKEN: ${{ steps.sonar.outputs.token }}
          SONAR_HOST_URL: http://127.0.0.1:9000
        with:
          args: >
            -Dsonar.projectKey=security-scan
            -Dsonar.sources=packages/,apps/
            -Dsonar.exclusions=**/node_modules/**,**/.pnpm-store/**

      # Wait for the Compute Engine task to finish before reading the gate —
      # otherwise project_status can return a stale or empty result from a prior
      # (or not-yet-computed) analysis. Mirrors the modular sonarqube.yml.
      - name: Wait for analysis task to complete
        env:
          SONAR_TOKEN: ${{ steps.sonar.outputs.token }}
        run: |
          if [ ! -f .scannerwork/report-task.txt ]; then
            echo "::error::report-task.txt not produced; analysis did not run"
            exit 1
          fi
          TASK_URL=$(grep '^ceTaskUrl=' .scannerwork/report-task.txt | cut -d'=' -f2-)
          if [ -z "$TASK_URL" ]; then
            echo "::error::ceTaskUrl missing from report-task.txt"; cat .scannerwork/report-task.txt; exit 1
          fi
          for i in $(seq 1 60); do
            STATUS=$(curl -fsS -u "${SONAR_TOKEN}:" "$TASK_URL" | python3 -c "import sys,json; print(json.load(sys.stdin)['task']['status'])")
            case "$STATUS" in
              SUCCESS) echo "Analysis complete after $((i*5))s"; break ;;
              FAILED|CANCELED) echo "::error::Analysis ended with status $STATUS"; exit 1 ;;
              *) echo "[$i/60] Analysis status: $STATUS"; sleep 5 ;;
            esac
            [ "$i" = "60" ] && { echo "::error::Analysis did not complete within 5 minutes"; exit 1; }
          done

      - name: Quality gate check (warn-only)
        id: qualitygate
        env:
          SONAR_TOKEN: ${{ steps.sonar.outputs.token }}
        run: |
          # Read the gate for the specific analysisId we just computed, not the
          # project's latest, so the result matches this run.
          ANALYSIS_ID=$(grep '^ceTaskId=' .scannerwork/report-task.txt | cut -d'=' -f2- \
            | xargs -I{} curl -fsS -u "${SONAR_TOKEN}:" "http://127.0.0.1:9000/api/ce/task?id={}" \
            | python3 -c "import sys,json; print(json.load(sys.stdin)['task'].get('analysisId',''))")
          if [ -n "$ANALYSIS_ID" ]; then
            curl -fsS -u "${SONAR_TOKEN}:" \
              "http://127.0.0.1:9000/api/qualitygates/project_status?analysisId=${ANALYSIS_ID}" > quality-gate.json
          else
            curl -fsS -u "${SONAR_TOKEN}:" \
              "http://127.0.0.1:9000/api/qualitygates/project_status?projectKey=security-scan" > quality-gate.json
          fi
          result=$(python3 -c "import sys,json; print(json.load(open('quality-gate.json'))['projectStatus']['status'])")
          echo "Quality gate: $result"
          if [ "$result" = "ERROR" ]; then
            echo "::warning::SonarQube quality gate FAILED. Warn-only; change exit 0 to exit 1 to enforce."
            exit 0  # change to `exit 1` to enforce the gate
          fi

      # 90-day compliance artifact (the prescribed evidence the modular
      # sonarqube.yml uploaded; retained for audit).
      - name: Upload SonarQube compliance artifact
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02  # v4
        with:
          name: sonarqube-${{ github.run_id }}
          path: |
            quality-gate.json
            .scannerwork/report-task.txt
          retention-days: 90
          if-no-files-found: warn
          include-hidden-files: true

  # --------------------------------------------------------------------------
  # Helm chart validity — blocking unless image_only
  # --------------------------------------------------------------------------
  helm-check:
    name: Helm Chart Validation
    runs-on: ubuntu-latest
    needs: [caller-lint]
    if: ${{ inputs.image_only != true }}
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4

      - name: Set up Helm
        uses: azure/setup-helm@1a275c3b69536ee54be43f2070a358922e12c8d4  # v4

      - name: Build chart dependencies
        env:
          HELM_CHART_PATH: ${{ inputs.helm_chart_path }}
        run: helm dependency build "./${HELM_CHART_PATH}"

      - name: helm lint
        env:
          HELM_CHART_PATH: ${{ inputs.helm_chart_path }}
          HELM_VALUES_FILE: ${{ inputs.helm_values_file }}
        run: helm lint "./${HELM_CHART_PATH}" -f "${HELM_VALUES_FILE}"

      - name: helm template (render check)
        env:
          HELM_RELEASE_NAME: ${{ inputs.helm_release_name }}
          HELM_CHART_PATH: ${{ inputs.helm_chart_path }}
          HELM_VALUES_FILE: ${{ inputs.helm_values_file }}
        run: helm template "${HELM_RELEASE_NAME}" "./${HELM_CHART_PATH}" -f "${HELM_VALUES_FILE}" > /tmp/rendered-chart.yaml

      # Restricted-PSS assertion on the rendered releases — a machine-checkable
      # re-scan of the templated securityContext rows. Ported from the modular
      # helm-validate.yml (which this workflow replaces) so that pod-security
      # enforcement isn't lost. Consumers with their own secctx make target
      # keep it; consumers without one (e.g. no root Makefile) fall back to
      # the gate's bundled checker below, so onboarding never requires
      # consumer-side tooling.
      - name: Resolve callee (ci-scans) ref
        id: calleeref
        env:
          WORKFLOW_REF: ${{ github.workflow_ref }}
          JOB_WF_SHA: ${{ github.job_workflow_sha }}
        run: |
          caller="${WORKFLOW_REF#"$GITHUB_REPOSITORY/"}"; caller="${caller%%@*}"
          ref=""
          if [ -f "$caller" ]; then
            ref="$(grep -oE 'reusable-security-gate\.yml@[^ ]+' "$caller" | head -1 | sed 's/.*@//')"
          fi
          ref="${ref:-${JOB_WF_SHA:-main}}"
          echo "ref=$ref" >> "$GITHUB_OUTPUT"

      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
        with:
          repository: c3-joshchiu/c3cdao-ci-scans
          ref: ${{ steps.calleeref.outputs.ref }}
          path: .ci-scans

      - name: Set up uv
        uses: astral-sh/setup-uv@d0cc045d04ccac9d8b7881df0226f9e82c39688e  # v6

      - name: Assert rendered pod-security controls (restricted PSS)
        env:
          SECCTX_MAKE_TARGET: ${{ inputs.secctx_make_target }}
        run: |
          set -uo pipefail
          if [ ! -f Makefile ]; then
            echo "::notice::no Makefile — running the gate's bundled restricted-PSS assertion"
            uv run .ci-scans/scripts/lib/assert_restricted_pss.py /tmp/rendered-chart.yaml
            exit 0
          fi
          set +e
          make_out=$(make -n -- "$SECCTX_MAKE_TARGET" 2>&1)
          make_rc=$?
          set -e
          if [ "$make_rc" -eq 0 ]; then
            make -- "$SECCTX_MAKE_TARGET"
            exit $?
          fi
          if printf '%s\n' "$make_out" | grep -Eqi "No rule to make target|No targets specified|No such file or directory"; then
            echo "::notice::consumer has no '${SECCTX_MAKE_TARGET}' make target — running the gate's bundled restricted-PSS assertion"
            uv run .ci-scans/scripts/lib/assert_restricted_pss.py /tmp/rendered-chart.yaml
            exit 0
          fi
          echo "::error::make -n ${SECCTX_MAKE_TARGET} failed (not a missing-target); refusing PSS fallback"
          printf '%s\n' "$make_out"
          exit 1

  # --------------------------------------------------------------------------
  # Cluster smoke — kind + helm install + GET /health against the built image.
  # helm-check (lint/template) is a static render; it can't catch a chart that
  # renders clean but crash-loops on boot (bad env, failed migration, broken
  # entrypoint). This boots the real image against an ephemeral Postgres.
  # Ported from the modular pr-gate.yml. Warn-only unless SECURITY_SCAN_BLOCKING.
  # --------------------------------------------------------------------------
  cluster-smoke:
    name: Cluster Smoke (kind + helm + /health)
    runs-on: ubuntu-latest
    needs: [phase1-build]
    if: ${{ inputs.image_only != true && needs.phase1-build.result == 'success' }}
    outputs:
      smoke_ok: ${{ steps.smoke_outcome.outputs.ok }}
    env:
      CLUSTER: ${{ inputs.cluster_name }}
      NAMESPACE: ${{ inputs.namespace }}
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4

      # kind node + pgvector + the loaded backend image strain the runner's
      # ~14 GB free disk; reclaim unused toolchains first (mirrors phase1-build).
      - name: Free disk space
        run: |
          sudo rm -rf /usr/share/dotnet /opt/ghc /usr/local/.ghcup \
            /usr/local/lib/android /opt/hostedtoolcache/CodeQL 2>/dev/null || true
          sudo docker image prune -af 2>/dev/null || true
          df -h /

      - name: Download image artifact (built in phase1-build)
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093  # v4
        with:
          name: ${{ env.IMAGE_ARTIFACT }}

      - name: Load image into local daemon
        run: docker load -i image.tar

      - name: Create kind cluster
        uses: helm/kind-action@ef37e7f390d99f746eb8b610417061a60e82a6cc  # v1.14.0
        with:
          cluster_name: ${{ inputs.cluster_name }}

      - name: Set up Helm
        uses: azure/setup-helm@1a275c3b69536ee54be43f2070a358922e12c8d4  # v4

      - name: kind install + helm deploy + /health smoke
        id: smoke
        # Warn-only: advisory unless SECURITY_SCAN_BLOCKING=true. Step outcome is
        # recorded below so security-gate can fail closed even when this step
        # continues on error (job result stays success).
        continue-on-error: ${{ vars.SECURITY_SCAN_BLOCKING != 'true' }}
        env:
          IMAGE: ${{ needs.phase1-build.outputs.image }}
          APP_PORT: ${{ inputs.app_port }}
          SERVICE_PORT: ${{ inputs.service_port }}
          HEALTH_PATH: ${{ inputs.health_path }}
          SMOKE_WORKLOAD_MATCH: ${{ inputs.smoke_workload_match }}
          HELM_CHART_PATH: ${{ inputs.helm_chart_path }}
          HELM_RELEASE_NAME: ${{ inputs.helm_release_name }}
          HELM_VALUES_FILE: ${{ inputs.helm_values_file }}
          HELM_VALUES_LOCAL_FILE: ${{ inputs.helm_values_local_file }}
        run: |
          set -uo pipefail
          fail() { echo "::warning::cluster-smoke: $1"; exit 1; }

          kind load docker-image "${IMAGE}" --name "${CLUSTER}" || fail "kind load failed"
          kubectl create namespace "${NAMESPACE}" --dry-run=client -o yaml | kubectl apply -f -

          # Ephemeral Postgres (chart does not provision a DB).
          cat <<'EOF' | kubectl -n "${NAMESPACE}" apply -f -
          apiVersion: apps/v1
          kind: Deployment
          metadata:
            name: app-postgres
          spec:
            replicas: 1
            selector: { matchLabels: { app: app-postgres } }
            template:
              metadata: { labels: { app: app-postgres } }
              spec:
                containers:
                  - name: postgres
                    image: pgvector/pgvector:pg16
                    env:
                      - { name: POSTGRES_USER, value: postgres }
                      - { name: POSTGRES_PASSWORD, value: postgres }
                      - { name: POSTGRES_DB, value: appdb }
                    ports: [{ containerPort: 5432 }]
          ---
          apiVersion: v1
          kind: Service
          metadata:
            name: app-postgres
          spec:
            selector: { app: app-postgres }
            ports: [{ port: 5432, targetPort: 5432 }]
          EOF

          kubectl -n "${NAMESPACE}" rollout status deploy/app-postgres --timeout=120s || fail "postgres not ready"

          # DATABASE_URL secret the chart expects (values-local: app-database-url).
          kubectl -n "${NAMESPACE}" create secret generic app-database-url \
            --from-literal=DATABASE_URL="postgresql://postgres:postgres@app-postgres:5432/appdb" \
            --dry-run=client -o yaml | kubectl apply -f -

          # The chart renders an HTTPRoute (Gateway API). kind ships no Gateway
          # API CRDs, so install them (server-side: these CRDs exceed the
          # client-side annotation limit). No controller is needed — the smoke
          # reaches the backend via port-forward, not the gateway.
          kubectl apply --server-side -f \
            https://github.com/kubernetes-sigs/gateway-api/releases/download/v1.2.1/standard-install.yaml \
            || fail "gateway-api CRD install failed"

          helm dependency build "./${HELM_CHART_PATH}" || fail "helm dependency build failed"

          # Install the app. The post-install migration hook runs alembic against
          # the Postgres above (from the backend image, pullPolicy Never — hence
          # SCAN_IMAGE must equal values-local's scan_image). Frontend
          # image isn't built here, so don't --wait on the whole release; we gate
          # on the backend rollout explicitly.
          helm install "${HELM_RELEASE_NAME}" "./${HELM_CHART_PATH}" \
            -n "${NAMESPACE}" \
            -f "${HELM_VALUES_FILE}" \
            -f "${HELM_VALUES_LOCAL_FILE}" \
            --timeout 5m || {
              echo "=== helm install failed — cluster diagnostics ==="
              kubectl -n "${NAMESPACE}" get pods,jobs -o wide || true
              kubectl -n "${NAMESPACE}" get events --sort-by=.lastTimestamp | tail -30 || true
              for p in $(kubectl -n "${NAMESPACE}" get pods -o name 2>/dev/null); do
                echo "--- describe ${p} ---"; kubectl -n "${NAMESPACE}" describe "${p}" | tail -25 || true
                echo "--- logs ${p} ---"; kubectl -n "${NAMESPACE}" logs "${p}" --all-containers --tail=40 || true
              done
              fail "helm install failed"
            }

          # `|| true`: grep exits 1 on no match and the step's errexit (GHA's
          # default `bash -e {0}`) would kill it here, skipping the fail-guard
          # below — the guard is the user-visible error. Workload name matching
          # is configurable via the smoke_workload_match input (default 'backend').
          backend_deploy=$(kubectl -n "${NAMESPACE}" get deploy -o name | grep -i "$SMOKE_WORKLOAD_MATCH" | head -1 || true)
          [ -n "${backend_deploy}" ] || fail "no backend deployment found"
          kubectl -n "${NAMESPACE}" rollout status "${backend_deploy}" --timeout=180s || {
            echo "=== backend not ready — diagnostics ==="
            kubectl -n "${NAMESPACE}" get pods -o wide || true
            kubectl -n "${NAMESPACE}" describe "${backend_deploy}" | tail -40 || true
            for p in $(kubectl -n "${NAMESPACE}" get pods -l app.kubernetes.io/component=backend -o name 2>/dev/null; kubectl -n "${NAMESPACE}" get pods -o name 2>/dev/null | grep -i "$SMOKE_WORKLOAD_MATCH"); do
              echo "--- describe ${p} ---"; kubectl -n "${NAMESPACE}" describe "${p}" | tail -30 || true
              echo "--- logs ${p} (current) ---"; kubectl -n "${NAMESPACE}" logs "${p}" --all-containers --tail=60 || true
              echo "--- logs ${p} (previous) ---"; kubectl -n "${NAMESPACE}" logs "${p}" --all-containers --previous --tail=60 || true
            done
            fail "backend not ready"
          }

          backend_svc=$(kubectl -n "${NAMESPACE}" get svc -o name | grep -i "$SMOKE_WORKLOAD_MATCH" | head -1 || true)
          [ -n "${backend_svc}" ] || fail "no backend service found"
          kubectl -n "${NAMESPACE}" port-forward "${backend_svc}" "${APP_PORT}:${SERVICE_PORT}" >/tmp/pf.log 2>&1 &
          pf_pid=$!
          sleep 6
          code=$(curl -s -o /dev/null -w '%{http_code}' "http://127.0.0.1:${APP_PORT}${HEALTH_PATH}" || echo 000)
          kill "${pf_pid}" 2>/dev/null || true
          echo "GET ${HEALTH_PATH} -> ${code}"
          [ "${code}" = "200" ] || fail "${HEALTH_PATH} returned ${code}, expected 200"
          echo "smoke OK"

      - name: Record smoke step outcome
        id: smoke_outcome
        if: always()
        run: |
          ok=false
          if [ "${{ steps.smoke.outcome }}" = "success" ]; then ok=true; fi
          echo "ok=$ok" >> "$GITHUB_OUTPUT"
          echo "cluster-smoke step outcome=${{ steps.smoke.outcome }} smoke_ok=$ok"

  # --------------------------------------------------------------------------
  # Vulnerability scan — Trivy + Grype × image + source SBOM (+ Grype image SBOM)
  # Source SBOM generated inline; image + image SBOM come from phase1-build.
  # --------------------------------------------------------------------------
  vuln-scan:
    name: Vulnerability Scan (Trivy + Grype)
    runs-on: ubuntu-latest
    needs: [phase1-build]
    if: needs.phase1-build.result == 'success'
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4

      - name: Label proxy-scan posture
        if: inputs.require_hardened_bases == false || inputs.require_hardened_bases == 'false'
        run: |
          msg='Proxy-scan posture (require_hardened_bases=false): this Vulnerability Scan covers the image as built with gate-reachable / consumer-specified bases. A green result is not proof the approved production image is clean — private-mirror / entitlement-unreachable OS-layer attestation stays with the consumer pipeline (IL5 / Game Warden / etc.).'
          echo "::warning::$msg"
          {
            echo "## Proxy-scan posture"
            echo ""
            echo "$msg"
          } >> "$GITHUB_STEP_SUMMARY"

      # Consumers aren't required to carry scanner config files — default to
      # empty ones so Trivy/Grype don't error on a missing path.
      - name: Default scanner configs when consumer lacks them
        run: |
          [ -f .trivyignore ] || : > .trivyignore
          [ -f .grype.yaml ] || echo 'ignore: []' > .grype.yaml

      - name: Download image artifact (built in phase1-build)
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093  # v4
        with:
          name: ${{ env.IMAGE_ARTIFACT }}

      - name: Load image into local daemon
        run: docker load -i image.tar

      - name: Generate source SBOM (pnpm + uv)
        uses: anchore/sbom-action@e22c389904149dbc22b58101806040fa8d37a610  # v0.24.0
        with:
          path: .
          format: cyclonedx-json
          output-file: sbom-source.json
          artifact-name: sbom-source

      - name: Download image SBOM (generated in phase1-build)
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093  # v4
        with:
          name: sbom-image


      - name: Trivy scan — image
        uses: aquasecurity/trivy-action@57a97c7e7821a5776cebc9bb87c984fa69cba8f1  # v0.35.0
        with:
          image-ref: ${{ needs.phase1-build.outputs.image }}
          format: table
          severity: ${{ env.TRIVY_SEVERITY }}
          exit-code: ${{ vars.SECURITY_SCAN_BLOCKING == 'true' && '1' || '0' }}
          # No ignore-unfixed: the modular pr-gate/sca-scan surfaced unfixed
          # HIGH/CRITICAL too, so we keep reporting them (no coverage reduction).
          trivyignores: .trivyignore

      - name: Trivy scan — source SBOM
        uses: aquasecurity/trivy-action@57a97c7e7821a5776cebc9bb87c984fa69cba8f1  # v0.35.0
        with:
          scan-type: sbom
          scan-ref: sbom-source.json
          severity: ${{ env.TRIVY_SEVERITY }}
          exit-code: ${{ vars.SECURITY_SCAN_BLOCKING == 'true' && '1' || '0' }}
          trivyignores: .trivyignore

      # No "Trivy scan — image SBOM" step: OS-package CVEs are already covered
      # authoritatively by "Trivy scan — image" above. Trivy cannot reliably
      # match OS packages from a third-party (Syft) SBOM — it warns as much and
      # returns 0 OS findings where the direct image scan finds them. Grype +
      # Syft are the same vendor (Anchore), so the image SBOM round-trips
      # cleanly to "Grype scan — image SBOM" below, which validates the artifact.

      - name: Grype scan — image
        uses: anchore/scan-action@e1165082ffb1fe366ebaf02d8526e7c4989ea9d2  # v7.4.0
        with:
          image: ${{ needs.phase1-build.outputs.image }}
          severity-cutoff: high
          fail-build: ${{ vars.SECURITY_SCAN_BLOCKING == 'true' }}
          output-format: table
          config: .grype.yaml

      - name: Grype scan — source SBOM
        uses: anchore/scan-action@e1165082ffb1fe366ebaf02d8526e7c4989ea9d2  # v7.4.0
        with:
          sbom: sbom-source.json
          severity-cutoff: high
          fail-build: ${{ vars.SECURITY_SCAN_BLOCKING == 'true' }}
          output-format: table
          config: .grype.yaml

      - name: Grype scan — image SBOM
        uses: anchore/scan-action@e1165082ffb1fe366ebaf02d8526e7c4989ea9d2  # v7.4.0
        with:
          sbom: sbom-image  # anchore/sbom-action stores artifact as file named after artifact-name
          severity-cutoff: high
          fail-build: ${{ vars.SECURITY_SCAN_BLOCKING == 'true' }}
          output-format: table
          config: .grype.yaml

  # --------------------------------------------------------------------------
  # Phase 1 (extra containers) — build each additional container image once.
  # Matrix over inputs.extra_containers; the job-level `if` short-circuits the
  # matrix so single-image consumers (empty input) skip this entirely and are
  # unaffected. Mirrors phase1-build (build + image SBOM + save/upload) with
  # per-leg artifact names and buildx cache scopes so parallel legs never
  # collide. context/image self-default in-expression (no renderer fills them).
  # No helm/cluster-smoke contract — extras are image-scan targets only.
  # --------------------------------------------------------------------------
  build-extra:
    name: Phase 1 extra — ${{ matrix.container.name }} image build
    runs-on: ubuntu-latest
    needs: [caller-lint]
    if: inputs.extra_containers != '' && inputs.extra_containers != '[]'
    strategy:
      fail-fast: false
      matrix:
        container: ${{ fromJSON(inputs.extra_containers) }}
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4

      # Resolve the leg's image tag once (context/image self-default since no
      # renderer normalizes the entry). Env-indirection avoids ${{ }} in shell.
      - name: Resolve leg image tag
        id: img
        env:
          RAW: ${{ matrix.container.image || format('{0}:local', matrix.container.name) }}
        run: echo "tag=$RAW" >> "$GITHUB_OUTPUT"

      # keep in sync with phase1-build "Free disk space"
      - name: Free disk space
        run: |
          sudo rm -rf /usr/share/dotnet /opt/ghc /usr/local/.ghcup \
            /usr/local/lib/android /opt/hostedtoolcache/CodeQL 2>/dev/null || true
          sudo docker image prune -af 2>/dev/null || true
          df -h /

      - uses: docker/setup-buildx-action@8d2750c68a42422c14e847fe6c8ac0403b4cbd6f  # v3

      # keep in sync with phase1-build "Login to hardened base registry"
      - name: Login to hardened base registries (Chainguard + Iron Bank)
        id: base-registry
        env:
          CGR_PULL_TOKEN: ${{ secrets.CGR_PULL_TOKEN }}
          CGR_PULL_USERNAME: ${{ secrets.CGR_PULL_USERNAME }}
          IRONBANK_TOKEN: ${{ secrets.IRONBANK_TOKEN }}
          IRONBANK_USERNAME: ${{ secrets.IRONBANK_USERNAME }}
          IRONBANK_REGISTRY: ${{ inputs.ironbank_registry }}
        run: |
          set -uo pipefail
          login_retry() { # <registry> <username> <token>
            local attempt=1 delay=15
            until printf '%s' "$3" | docker login "$1" -u "$2" --password-stdin; do
              if [ "$attempt" -ge 4 ]; then echo "$1 login failed after 4 attempts" >&2; return 1; fi
              echo "$1 login failed (attempt $attempt/4); retrying in ${delay}s..." >&2
              sleep "$delay"; delay=$((delay * 2)); attempt=$((attempt + 1))
            done
          }
          builder='${{ inputs.builder_image }}'
          runtime='${{ inputs.runtime_image }}'
          cgr_ok=0; ib_ok=0
          if [ -n "${CGR_PULL_TOKEN:-}" ]; then
            login_retry cgr.dev "$CGR_PULL_USERNAME" "$CGR_PULL_TOKEN"
            echo "Hardened bases: Chainguard (cgr.dev) authenticated"
            cgr_ok=1
          fi
          if [ -n "${IRONBANK_TOKEN:-}" ]; then
            login_retry "$IRONBANK_REGISTRY" "$IRONBANK_USERNAME" "$IRONBANK_TOKEN"
            echo "Hardened bases: Iron Bank (${IRONBANK_REGISTRY}) authenticated"
            ib_ok=1
          fi
          # Primary base failover: only swap the primary's builder/runtime to the
          # ironbank_* images when Chainguard is absent but Iron Bank is present
          # (preserves the original single-tier behavior). With both authenticated,
          # the primary keeps its Chainguard bases and extra images pull their own
          # Iron Bank bases from the now-logged-in registry.
          if [ "$cgr_ok" = 0 ] && [ "$ib_ok" = 1 ]; then
            [ -n '${{ inputs.ironbank_builder_image }}' ] && builder='${{ inputs.ironbank_builder_image }}'
            [ -n '${{ inputs.ironbank_runtime_image }}' ] && runtime='${{ inputs.ironbank_runtime_image }}'
            echo "::notice::CGR_PULL_TOKEN not configured — using Iron Bank (${IRONBANK_REGISTRY}) for primary bases"
          fi
          if [ "$cgr_ok" = 0 ] && [ "$ib_ok" = 0 ]; then
            if [ '${{ inputs.require_hardened_bases }}' = 'true' ]; then
              echo "::error::No hardened-registry credentials — add CGR_PULL_TOKEN/CGR_PULL_USERNAME (Chainguard) and/or IRONBANK_TOKEN/IRONBANK_USERNAME (Iron Bank), or set require_hardened_bases: false to accept consumer-specified bases."
              exit 1
            fi
            echo "::warning::Proxy-scan posture: no hardened-registry credentials and require_hardened_bases=false — building on consumer-specified bases without a registry login. Vulnerability Scan will cover this gate-reachable image only; a green result is not proof the approved production image is clean."
          fi
          echo "builder_image=$builder" >> "$GITHUB_OUTPUT"
          echo "runtime_image=$runtime" >> "$GITHUB_OUTPUT"

      - name: Label proxy-scan posture
        if: inputs.require_hardened_bases == false || inputs.require_hardened_bases == 'false'
        run: |
          msg='Proxy-scan posture (require_hardened_bases=false): build-extra builds with gate-reachable / consumer-specified bases. Downstream Vulnerability Scan extra is not an approved-prod attestation.'
          echo "::warning::$msg"
          {
            echo "## Proxy-scan posture"
            echo ""
            echo "$msg"
          } >> "$GITHUB_STEP_SUMMARY"

      # Base-image args come from the login step; the primary APP_*/RUNTIME_APKS
      # args are backend-specific, so extra containers carry their own overrides
      # via matrix.container.build_args (a newline-joined string).
      - name: Build extra image (local load, no push)
        uses: docker/build-push-action@10e90e3645eae34f1e60eeb005ba3a3d33f178e8  # v6
        with:
          context: ${{ matrix.container.context || '.' }}
          file: ${{ matrix.container.dockerfile }}
          target: ${{ matrix.container.target }}
          push: false
          load: true
          tags: ${{ steps.img.outputs.tag }}
          platforms: linux/amd64
          build-args: |
            BUILDER_IMAGE=${{ steps.base-registry.outputs.builder_image }}
            RUNTIME_IMAGE=${{ steps.base-registry.outputs.runtime_image }}
            ${{ matrix.container.build_args }}
          cache-from: type=gha,scope=security-scan-${{ matrix.container.name }}
          cache-to: type=gha,mode=max,scope=security-scan-${{ matrix.container.name }}
          provenance: false

      - name: Generate image SBOM (from local daemon)
        uses: anchore/sbom-action@e22c389904149dbc22b58101806040fa8d37a610  # v0.24.0
        with:
          image: ${{ steps.img.outputs.tag }}
          format: cyclonedx-json
          output-file: sbom-image-${{ matrix.container.name }}.json
          artifact-name: sbom-image-${{ matrix.container.name }}

      - name: Save image to tarball
        env:
          IMG: ${{ steps.img.outputs.tag }}
        run: docker save "$IMG" -o image.tar

      - name: Upload image artifact
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02  # v4
        with:
          name: scan-image-${{ matrix.container.name }}
          path: image.tar
          if-no-files-found: error
          retention-days: 1

  # --------------------------------------------------------------------------
  # Vulnerability scan (extra containers) — image-only Trivy + Grype per leg.
  # Source SBOM + source scans run once in vuln-scan (whole-repo), so these legs
  # cover the built image only: Trivy image, Grype image, Grype image-SBOM.
  # Same SECURITY_SCAN_BLOCKING exit-code semantics as the primary vuln-scan.
  # --------------------------------------------------------------------------
  vuln-scan-extra:
    name: Vulnerability Scan extra — ${{ matrix.container.name }}
    runs-on: ubuntu-latest
    needs: [build-extra]
    # build-extra is skipped when the input is empty (result != 'success'), which
    # also short-circuits this job's matrix — no fromJSON('') at plan time.
    if: needs.build-extra.result == 'success'
    strategy:
      fail-fast: false
      matrix:
        container: ${{ fromJSON(inputs.extra_containers) }}
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4

      - name: Label proxy-scan posture
        if: inputs.require_hardened_bases == false || inputs.require_hardened_bases == 'false'
        run: |
          msg='Proxy-scan posture (require_hardened_bases=false): this Vulnerability Scan covers the image as built with gate-reachable / consumer-specified bases. A green result is not proof the approved production image is clean — private-mirror / entitlement-unreachable OS-layer attestation stays with the consumer pipeline (IL5 / Game Warden / etc.).'
          echo "::warning::$msg"
          {
            echo "## Proxy-scan posture"
            echo ""
            echo "$msg"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Resolve leg image tag
        id: img
        env:
          RAW: ${{ matrix.container.image || format('{0}:local', matrix.container.name) }}
        run: echo "tag=$RAW" >> "$GITHUB_OUTPUT"

      # keep in sync with vuln-scan "Default scanner configs when consumer lacks them"
      - name: Default scanner configs when consumer lacks them
        run: |
          [ -f .trivyignore ] || : > .trivyignore
          [ -f .grype.yaml ] || echo 'ignore: []' > .grype.yaml

      - name: Download image artifact (built in build-extra)
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093  # v4
        with:
          name: scan-image-${{ matrix.container.name }}

      - name: Load image into local daemon
        run: docker load -i image.tar

      - name: Download image SBOM (generated in build-extra)
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093  # v4
        with:
          name: sbom-image-${{ matrix.container.name }}

      - name: Trivy scan — image
        uses: aquasecurity/trivy-action@57a97c7e7821a5776cebc9bb87c984fa69cba8f1  # v0.35.0
        with:
          image-ref: ${{ steps.img.outputs.tag }}
          format: table
          severity: ${{ env.TRIVY_SEVERITY }}
          exit-code: ${{ vars.SECURITY_SCAN_BLOCKING == 'true' && '1' || '0' }}
          trivyignores: .trivyignore

      - name: Grype scan — image
        uses: anchore/scan-action@e1165082ffb1fe366ebaf02d8526e7c4989ea9d2  # v7.4.0
        with:
          image: ${{ steps.img.outputs.tag }}
          severity-cutoff: high
          fail-build: ${{ vars.SECURITY_SCAN_BLOCKING == 'true' }}
          output-format: table
          config: .grype.yaml

      - name: Grype scan — image SBOM
        uses: anchore/scan-action@e1165082ffb1fe366ebaf02d8526e7c4989ea9d2  # v7.4.0
        with:
          sbom: sbom-image-${{ matrix.container.name }}  # download-artifact writes the file named after artifact-name
          severity-cutoff: high
          fail-build: ${{ vars.SECURITY_SCAN_BLOCKING == 'true' }}
          output-format: table
          config: .grype.yaml

  # --------------------------------------------------------------------------
  # Security Gate — aggregate required check
  # --------------------------------------------------------------------------
  security-gate:
    name: Security Gate
    runs-on: ubuntu-latest
    needs: [caller-lint, phase1-build, secrets-scan, sast-semgrep, sast-sonarqube, helm-check, cluster-smoke, vuln-scan, build-extra, vuln-scan-extra]
    if: always()
    steps:
      # Consumer checkout so we can parse the caller's uses: pin. job_workflow_sha
      # is empty on pull_request-triggered reusable calls (same as caller-lint).
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4

      - name: Resolve callee (ci-scans) ref
        id: calleeref
        env:
          WORKFLOW_REF: ${{ github.workflow_ref }}
          JOB_WF_SHA: ${{ github.job_workflow_sha }}
        run: |
          caller="${WORKFLOW_REF#"$GITHUB_REPOSITORY/"}"; caller="${caller%%@*}"
          ref=""
          if [ -f "$caller" ]; then
            ref="$(grep -oE 'reusable-security-gate\.yml@[^ ]+' "$caller" | head -1 | sed 's/.*@//' | tr -d '#')"
            ref="${ref%%[[:space:]]*}"
          fi
          ref="${ref:-${JOB_WF_SHA:-main}}"
          echo "ref=$ref" >> "$GITHUB_OUTPUT"
          echo "::notice::security-gate: evaluating with ci-scans ref '$ref'"

      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
        with:
          repository: c3-joshchiu/c3cdao-ci-scans
          ref: ${{ steps.calleeref.outputs.ref }}
          path: .ci-scans

      - name: Evaluate scan results
        env:
          NEEDS_JSON: ${{ toJSON(needs) }}
          EXTRA_CONTAINERS: ${{ inputs.extra_containers }}
          IMAGE_ONLY: ${{ inputs.image_only }}
        run: python3 .ci-scans/scripts/lib/evaluate_security_gate.py
