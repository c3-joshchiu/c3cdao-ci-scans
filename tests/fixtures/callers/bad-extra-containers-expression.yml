# bad: expression-valued extra_containers (not statically lintable)
name: Security Scan
on:
  pull_request:
    branches: [main]
permissions:
  contents: read
  pull-requests: write
  actions: write
jobs:
  security-scan:
    permissions:
      contents: read
      pull-requests: write
      actions: write
    uses: c3-joshchiu/c3cdao-ci-scans/.github/workflows/reusable-security-gate.yml@main
    secrets:
      CGR_PULL_TOKEN: ${{ secrets.CGR_PULL_TOKEN }}
      CGR_PULL_USERNAME: ${{ secrets.CGR_PULL_USERNAME }}
      IRONBANK_TOKEN: ${{ secrets.IRONBANK_TOKEN }}
      IRONBANK_USERNAME: ${{ secrets.IRONBANK_USERNAME }}
    with:
      scan_image: app:local
      dockerfile: containers/backend/Dockerfile
      extra_containers: ${{ vars.EXTRA_CONTAINERS }}
