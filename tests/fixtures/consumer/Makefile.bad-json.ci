# Lint fixture contract variant: ci-manifest prints invalid JSON
# (ci-contract-manifest must fire, blocking).

SHELL := /bin/bash

.PHONY: ci-manifest ci-build ci-secctx ci-smoke-env

ci-manifest:
	@echo 'not json {'

ci-build:
	@echo noop

ci-secctx:
	@echo noop

ci-smoke-env:
	@echo noop
